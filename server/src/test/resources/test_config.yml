server:
  applicationConnectors:
    - type: http
      port: 0

logging:

  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: INFO

  # Logger-specific levels.
  loggers:
    io.github.sullis.netflix: DEBUG

aggregatorConcurrencyLevel: 100
